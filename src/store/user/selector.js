export const selectToken = (state) => state.user.token;

export const selectUser = (state) => state.user;

export const selectLatitude = (state) => state.user.latitude;

export const selectLongitude = (state) => state.user.longitude;
